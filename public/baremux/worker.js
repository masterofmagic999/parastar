/**
 * Bare-Mux Worker
 * Handles transport switching for the proxy (wisp/bare protocols)
 */

// Import bare-mux from CDN
importScripts('https://unpkg.com/@mercuryworkshop/bare-mux@2.1.8/dist/index.js');

console.log('Bare-mux worker loaded');
